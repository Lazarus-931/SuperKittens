"""Normalization bindings."""

def rmsnorm(x, weight, eps=1e-5, out=None):
    raise NotImplementedError("bindings not yet wired — kernel library in progress")

def layernorm(x, weight, bias=None, eps=1e-5, out=None):
    raise NotImplementedError("bindings not yet wired — kernel library in progress")
