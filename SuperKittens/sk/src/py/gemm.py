"""GEMM bindings."""

def gemm(a, b, bias=None, activation=None, out=None):
    """Matrix multiply with optional bias + activation fusion."""
    raise NotImplementedError("bindings not yet wired — kernel library in progress")
