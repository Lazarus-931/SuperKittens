"""SuperKittens serving entrypoints.

Opt-in, additive serving drivers that consume the in-tree C ABI. Nothing here
is imported by the single-mini forward/decode/prefill paths; importing this
package has no effect on them.
"""
