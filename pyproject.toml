[project]
name = "superkittens-mamba-bench"
version = "0.1.0"
description = "Python environment for SuperKittens MLX benchmark scripts."
requires-python = ">=3.11"
dependencies = [
    "mlx",
    "numpy",
]

[tool.uv]
package = false
